# 紫微斗数网页工具 PRD

| 项目 | 内容 |
| --- | --- |
| 产品名称 | 紫微斗数排盘与解读工具（暂定名：**紫微鉴 / Ziwei Lab**） |
| 文档版本 | v1.2 |
| 文档状态 | 待评审 |
| 创建日期 | 2026-09-05 |
| 目标形态 | 移动端优先的 Web 应用（响应式 + PWA），纯前端排盘 |
| 目标上线 | Beta 6 周 / 正式版 v1.0 8 周（详见第 13 章） |

## 修订记录

| 版本 | 日期 | 修订人 | 说明 |
| --- | --- | --- | --- |
| v1.0 | 2026-09-05 | — | 初稿，定义目标、范围、功能需求与技术方案 |
| v1.1 | 2026-09-05 | — | 新增视觉设计规范（第 6 章）、增长与获客（第 11 章）、数据埋点与指标体系（第 12 章）；补充 P1 功能详细规格（4.6）、状态设计与交互流程（7.3–7.5）、AI 接口契约与存储设计（9.5–9.7）、发布与灰度策略（13.2–13.3）；新增风险 R8–R11、待确认 Q7–Q12、验收 15.3–15.5；追加设计令牌落地代码（附录 C） |
| v1.2 | 2026-09-05 | — | 补充功能优先级矩阵（4.2）、黄金样本集构成明细（5.3）、动效与微交互规范（6.8）、桌面端与平板布局（7.6）、引擎 API 设计（9.8）、测试策略与 CI/CD（9.9）、内容生产流程与审校机制（10.1）、冷启动策略（11.7）；新增命名与 slug 规范（附录 D）；统一术语与路由格式 |

---

## 1. 项目概述

### 1.1 背景

紫微斗数是华人圈使用最广的命理体系之一，核心是「以出生时间起盘，用十四主星 + 辅曜 + 四化在十二宫的分布来推演性格与运势」。线上工具大量存在，但普遍存在三类问题：

1. **排盘不准**：忽略真太阳时（出生地经度）、农历闰月、早子/晚子时换日规则，导致命宫错位。
2. **黑盒不可解释**：只给结论（"你是天同星，性格温和"），不展示"这颗星为什么在这里、依据是什么"，用户无法判断可信度。
3. **流派混用不标注**：三合派（星性为主）与四化派（飞星四化为主）规则不同，多数工具混用却不明示。

同时排盘本质是**确定性计算**（输入 → 唯一命盘），非常适合纯前端实现：零后端成本、数据不出端、响应即时。

### 1.2 产品定位

> **一个准确、可解释、隐私优先的现代紫微斗数排盘工具。**

三条差异化支柱：

| 支柱 | 含义 | 用户可感知的价值 |
| --- | --- | --- |
| **准确** | 真太阳时校正、闰月/早子晚子严格处理，规则引擎经 200+ 黄金样本回归验证 | "我这张盘是对的" |
| **可解释** | 每颗星、每个格局、每条结论都能下钻到"安星规则 + 宫位组合 + 流派出处" | "我知道它为什么这么说" |
| **隐私优先** | 排盘全程在浏览器完成，出生数据不上传；AI 解读需用户显式授权 | "我敢输入真实生日" |

### 1.3 产品目标与成功指标

| 层级 | 指标 | 目标值 |
| --- | --- | --- |
| 北极星 | 周活跃排盘用户（WAU） | MVP 后 3 个月 5,000 |
| 质量 | 排盘引擎黄金样本通过率 | 100%（200+ 样本，CI 强制） |
| 质量 | 排盘计算耗时 | < 50ms（中端安卓机） |
| 体验 | 移动端 Lighthouse Performance | ≥ 85 |
| 体验 | LCP | < 2.5s（4G 网络） |
| 参与 | 排盘完成率（进入表单 → 看到命盘） | ≥ 75% |
| 参与 | 盘面分享率 | ≥ 15% |
| 参与 | 次日/7 日留存 | 25% / 10% |
| 深度 | AI 深度解读渗透率（P1） | ≥ 30% |
| 内容 | 星曜/宫位释义覆盖率 | 100%（14 主星 + 18 辅曜 + 12 宫位） |

### 1.4 非目标（明确不做）

- 不做付费预测、改运、法事、开运商品等交易型业务。
- 不做医疗、法律、金融投资建议；相关内容在 UI 层与文案层双重拦截。
- 不做社区八卦/陌生人社交（P2 前不考虑）。
- 不支持 1900 年前 / 2100 年后的出生时间（历法库边界，越界明确提示）。

---

## 2. 用户与场景

### 2.1 用户画像

| 画像 | 占比预估 | 特征 | 核心诉求 | 付费意愿 |
| --- | --- | --- | --- | --- |
| **A. 好奇小白** | 60% | 20–35 岁，女性偏多，通过社交平台/朋友分享进入 | "我是什么命？性格、感情、今年运势怎么样" | 低（愿为"详细报告"小额付费） |
| **B. 命理爱好者** | 30% | 25–45 岁，有自学基础，会对照多个工具验盘 | "排盘要准，流派要清楚，术语要能查" | 中（愿为多流派、导出、无广告付费） |
| **C. 从业者/命理师** | 10% | 需要给客户排盘、留存档案、导出底稿 | "快速排盘 + 客户管理 + 可导出解读" | 高（工具类订阅） |

### 2.2 核心场景（JTBD）

| # | 场景 | 用户语言 | 当前痛点 | 我们的解法 |
| --- | --- | --- | --- | --- |
| S1 | 排一张准盘 | "我生日是农历闰五月，工具都排错了" | 闰月/真太阳时/早子晚子处理错误 | 表单显式处理闰月与子时换日，出生地经纬度校正真太阳时，并在盘面标注"已按真太阳时校正 +X 分钟" |
| S2 | 看懂自己的盘 | "一屏幕的星，看不懂" | 只有盘面，没有引导 | 命盘 → 点击任意宫位 → 白话解读 + 三方四正 + 该宫涉及的格局 |
| S3 | 看某年运势 | "我今年适不适合换工作？" | 只有"一生命运"笼统描述 | 大限 × 流年 × 流月三层盘，回答限定在"某年某宫"范围 |
| S4 | 看两人关系 | "我们俩合不合？" | 多数工具不支持合盘 | 合盘：双方命宫主星配对 + 四化互动 + 关系维度（情感/沟通/合作） |
| S5 | 对照流派差异 | "三合派和四化派说法不一样，信哪个" | 工具黑盒，不知用了哪派 | 流派开关（三合派星性 / 四化派飞星），结论标注流派出处 |
| S6 | 管理与复用 | "我给全家人都排过，找不到了" | 无历史、无分享 | 本地盘库 + 标签备注 + 链接分享（参数编码，不含姓名） |

### 2.3 关键用户旅程（主流程，目标 ≤ 3 步出盘）

```
落地页 → 填写生日（默认阳历/北京时间）
      → [选填] 出生地（用于真太阳时）→ 选择性别
      → 点击【开始排盘】
      → 命盘页（十二宫格 + 底部 Tab：总览/详解/运势/格局）
      → 点击任一宫位 → 宫位详情抽屉（星曜组合 + 白话解读 + 依据）
      → [可选] 一键 AI 深度解读 / 保存 / 分享
```

**设计约束**：主流程必须能在 30 秒内完成；出生地、真太阳时、农历等高级选项折叠在"高级设置"中，默认给最佳实践值。

---

## 3. 竞品分析

| 竞品 | 形态 | 优势 | 不足 | 我们的机会 |
| --- | --- | --- | --- | --- |
| 拾星集 | 多术数聚合站（紫微/八字/六爻/奇门）+ AI | 功能最全、AI 解读成熟 | 盘面陈旧、移动端体验差、规则不透明 | 移动端体验 + 可解释性 |
| zwds365 | 纯前端排盘 + AI 解读 | 纯前端、响应快 | 解读模板化、无流派、无合盘 | 可解释 + 多流派 + 合盘 |
| 紫微鉴（ziweijian） | 匿名本地生成，给出"3 条基础解读 + 依据" | 隐私优先、已有"依据"意识 | 解读深度极浅、无流年/合盘 | 深化"依据链"，做到结论逐条可下钻 |
| 青囊、bagua 等 | 排盘 + 格局/三元 | 有格局识别、大限流年时间轴 | 同质化严重、UI 传统 | 交互现代化 + 场景化提问 |
| astrologybazi（英文） | 英文紫微排盘，66 星 | 出海/多语言 | 中文圈无影响力 | 后续多语言（P2） |

#### 3.1 功能对比矩阵

✅ 完整支持 / 🟡 部分支持或深度有限 / ❌ 不支持

| 能力 | 拾星集 | zwds365 | 紫微鉴 | 青囊 | **本产品目标** |
| --- | --- | --- | --- | --- | --- |
| 基础排盘 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 农历闰月 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 真太阳时校正 | ✅ | ✅ | 🟡 | ✅ | ✅ **并明示校正分钟数** |
| 早子/晚子策略可选 | ❌ | ❌ | ❌ | ❌ | ✅ |
| 庙旺亮度标注 | ✅ | ✅ | 🟡 | ✅ | ✅ |
| 大限 / 小限 | ✅ | ✅ | 🟡 | ✅ | ✅ |
| 流年 / 流月 | 🟡 | ✅ | ❌ | ✅ | ✅ |
| 格局自动识别 | ✅ | ✅ | 🟡（3 条） | ✅ | ✅ 60+ 且带成立条件 |
| **结论依据可下钻** | ❌ | ❌ | 🟡 | ❌ | ✅ 核心差异 |
| **流派切换** | ❌ | ❌ | ❌ | ❌ | ✅ |
| **排盘准确性可公开验证** | ❌ | ❌ | ❌ | ❌ | ✅ 样本集 + 与 iztro 的 diff 报告 |
| 合盘 | ❌ | ❌ | ❌ | 🟡 | ✅ |
| AI 深度解读 | ✅ | ✅ | ❌ | ❌ | ✅ 规则约束 + 事实层可控 |
| **纯前端 / 数据不出端** | ❌ | ✅ | ✅ | ❌ | ✅ |
| 术语词典 | ❌ | ❌ | ❌ | 🟡 | ✅ |
| 导出 PNG / PDF | 🟡 | ❌ | ❌ | ❌ | ✅ |
| 移动端体验 | 弱 | 中 | 中 | 弱 | ✅ 移动优先 |

**结论**：功能层面已是红海（排盘 + AI 解读人人都有），**蓝海在"可信"**——把"为什么"做成产品能力，而非把"是什么"堆得更多。上表中标粗的四项是竞品集体缺失、且短期难以补齐的能力，构成本产品的护城河。

---

## 4. 功能需求

### 4.1 功能地图

```
紫微斗数网页工具
├─ 排盘（P0）
│   ├─ 输入：阳历/农历、闰月、时辰（含"不确定"）、性别、出生地
│   ├─ 真太阳时校正（开关 + 可视化说明）
│   └─ 早子时/晚子时换日策略
├─ 命盘（P0）
│   ├─ 十二宫格渲染（宫名、宫干支、主星、辅曜、四化角标、大限、身宫）
│   ├─ 宫位详情抽屉（星曜释义、三方四正、本宫/对宫/三合）
│   └─ 术语悬浮解释（星曜、四化、宫位、神煞）
├─ 解读（P0 基础 / P1 AI）
│   ├─ 命宫主星人格、十二宫简述、格局摘要
│   └─ AI 深度解读（授权后，流式输出）
├─ 时间维度（P0 大限 / P1 流年流月）
│   └─ 大限（十年一运）→ 流年 → 流月，三层盘切换
├─ 格局库（P1）
├─ 合盘（P1）
├─ 盘库与分享（P0 本地 / P1 导出）
└─ 设置与合规（P0）
```

### 4.2 优先级定义

- **P0**：MVP 必须有，缺失则产品不成立。
- **P1**：首个正式版（v1.0）应有，影响留存与传播。
- **P2**：后续迭代，需数据验证后再投入。

#### 功能优先级矩阵（价值 / 成本 / 差异化）

| 功能 | 用户价值 | 实现成本 | 差异化贡献 | 决策 |
| --- | --- | --- | --- | --- |
| 排盘准确性 | 高 | 高 | **高** | P0 必做 |
| 十二宫格渲染 | 高 | 中 | 低（人人都有） | P0 必做 |
| **宫位详情与依据链** | 高 | 中 | **高** | P0 必做 |
| 真太阳时校正 | 中 | 低 | 中 | P0 必做 |
| 大限推演 | 高 | 中 | 低 | P0 必做 |
| 盘库与分享 | 中 | 低 | 中 | P0 必做 |
| 流年 / 流月 | 高 | 中 | 低 | P1 |
| AI 深度解读 | 高 | 中 | 低（竞品已普及） | P1 |
| 格局库 | 中 | 中 | 中 | P1 |
| 合盘 | 中 | 中 | 中 | P1 |
| **术语词典 / SEO 词条页** | 中 | 低 | 中 | **P1，但与获客强耦合 → 内容资产需从 M1 起并行** |
| 多流派切换 | 低（大众）/ 高（爱好者） | 高 | **高** | P1 |

**结论**：P0 的核心是"排得准 + 看得懂"，P1 补齐"深度与传播"。其中**术语词典与 SEO 词条页共用同一份内容资产**，是全表投入产出比最高的一项——一次生产，同时服务产品体验与自然流量。

### 4.3 P0 详细需求

#### F1 排盘输入

| 字段 | 必填 | 规则 |
| --- | --- | --- |
| 历法类型 | 是 | 阳历 / 农历 切换，默认阳历 |
| 出生日期 | 是 | 支持 1900-01-01 ~ 2100-12-31；农历需支持闰月（如"闰五月"独立选项） |
| 出生时间 | 是 | 十二时辰下拉（子 23-01 时，同时拆分为早子 00-01 / 晚子 23-00）；提供"不知道时辰"入口 → 引导使用"问事/定盘"或按默认午时并强提示误差 |
| 性别 | 是 | 男 / 女（影响大限顺逆与部分星曜安布） |
| 出生地 | 否 | 省市区三级 → 自动带出经纬度，用于真太阳时校正；默认"北京（东经 116.4°）" |
| 真太阳时校正 | 否 | 默认开启；关闭时明确提示"可能与实际命盘存在偏差" |
| 时区 | 否 | 默认 Asia/Shanghai（UTC+8）；海外出生支持选择时区 |

**关键规则**：
- 真太阳时 = 平太阳时（地方经度时差） + 真平太阳时差（均时差，查表/公式）。需在盘面顶部以"已校正 +X 分钟"明示。
- 早子时（00:00–01:00）与晚子时（23:00–24:00）：提供「晚子时归当日 / 晚子时归次日」开关，默认"晚子时归次日"，并在盘面标注所采用策略。
- 输入校验失败时，错误提示需具体（如"农历闰五月只在闰年存在，请确认年份"），不允许静默纠正。

#### F2 命盘渲染

- **布局**：传统 4×4 十二宫格（中宫显示命主、身主、五行局、四柱、性别、真太阳时信息）；移动端可切换为列表模式（按宫位顺序纵向排列，便于阅读）。
- **单宫内容**：
  - 左上：大限年龄区间（如 `24-33`）
  - 右上：宫位天干地支（如 `甲子`）
  - 中部：十四主星（大号字），四化以角标渲染（禄 / 权 / 科 / 忌 四色）
  - 下部：辅曜、杂曜（小号字，超过 N 颗时折叠为「+N」）
  - 身宫：宫格外框高亮 + 徽标
- **视觉语义**：主星统一用墨色，星系（北斗 / 中天 / 南斗）不额外着色，避免盘面花哨；星曜亮度用 6.2.4 五态标签色、四化用 6.2.5 四化角标色；当前大限/流年宫位加高亮边框。完整视觉规格见第 6 章。
- **交互**：
  - 点击宫位 → 底部抽屉展示详情（移动端）/ 侧栏（桌面端）
  - 顶部三段切换：本命盘 / 大限盘 / 流年盘
  - 四化飞星线：P0 可选（默认关闭，设置中开启，避免信息过载）
- **无障碍**：宫格可键盘聚焦，星曜提供 `aria-label`（如"紫微星，化禄"）。

#### F3 宫位详情

每个宫位详情包含：

1. **本宫概览**：宫名 + 宫干支 + 主星组合 + 是否为身宫/大限所在。
2. **星曜释义**：逐颗列出（主星详情，辅曜简述），点击跳转术语词典。
3. **三方四正**：本宫 + 对宫 + 三合宫的星曜联动说明（三合派核心）。
4. **四化影响**：本宫宫干四化飞入哪些宫、生年四化落在此宫的星曜。
5. **白话解读**：按维度组织（性格 / 事业 / 财运 / 感情 / 健康——健康维度仅作"养生提示"，不作诊断）。
6. **依据链**：可展开的"规则依据"（如"天同在戌：由五行局土五局、紫微在寅推定"）。

#### F4 基础解读

- 命宫主星人格（14 主星 × 独坐/双星组合，共约 30+ 组合文案）
- 身宫含义
- 十二宫一句话速读
- 自动识别格局摘要（P0 支持 20 个最常见格局，P1 扩展至 60+）
- 所有结论需带**免责声明条**（首次出现时展示，可折叠）

#### F5 盘库与分享

- 本地保存（localStorage / IndexedDB），支持多张盘、备注昵称（非真实姓名）、标签（家人/朋友/客户）
- 分享：链接编码排盘参数（Base64/短参数串），打开即还原；链接不含姓名与备注
- 导出图片（P0 简单版：盘面 PNG）

### 4.4 P1 需求

| ID | 功能 | 要点 |
| --- | --- | --- |
| F6 | **AI 深度解读** | 用户显式授权后才发起；仅上传**结构化盘面 JSON**（星曜分布、四化、大限流年），不上传姓名/备注/设备信息；模板化 Prompt + 输出结构约束（分维度、分点、带依据引用）；流式输出；敏感话题（医疗/法律/投资/生死）拒答并引导；结果可反馈"不准确" |
| F7 | **流年/流月运势** | 流年盘（太岁宫 + 流年四化 + 流年斗君）、流月；与大限叠加显示；支持"今年/明年"快速切换 |
| F8 | **格局库** | 自动识别 ≥ 60 个格局（如紫府朝垣、日月并明、极向离明、杀破狼、机月同梁、阳梁昌禄等），给出成立条件、宫位、吉凶倾向、古籍出处 |
| F9 | **合盘** | 双人盘：命宫主星配对、四化互动、十二宫对应关系（情感/沟通/合作/亲子 四种模式），输出"契合点 + 摩擦点 + 建议" |
| F10 | **多流派切换** | 三合派（星性为主）/ 四化派（飞星四化为主）切换，差异处明确标注；进阶：中州派安星细节开关 |
| F11 | **导出与打印** | 高清 PNG、PDF 解读报告（含盘面 + 分宫解读 + 免责页） |
| F12 | **术语词典** | 星曜、四化、宫位、神煞、格局的可搜索词典，支持从盘面任意处跳转 |

### 4.5 P2 需求（暂不排期）

- 会员订阅（AI 解读次数、多流派、去广告）
- 从业者工作台：客户盘管理、批量排盘、自定义解读模板
- 紫微日历 / 择吉
- 社区问答与盘例讨论（需内容审核成本）
- 多语言：繁体中文、English（英文需重做术语体系）
- PWA 离线安装、桌面端专业布局

### 4.6 P1 功能详细规格

#### F6 AI 深度解读

**流程**：宫位详情 / 总览页点击「AI 深度解读」→ 授权弹窗（明示将上传的内容、不含姓名）→ 生成（流式，预期 8–15s）→ 结果页（可复制/导出/反馈）。

| 项目 | 规格 |
| --- | --- |
| 上传内容 | 结构化盘面 JSON：十四主星落宫与亮度、辅曜、生年四化、宫干四化、五行局、大限流年、识别到的格局。**不含**姓名、备注、出生时间原始值、设备标识 |
| 输出结构 | 固定六段：① 命格总览 ② 性格特质 ③ 事业财运 ④ 感情人际 ⑤ 当前大限与流年 ⑥ 建议与提醒。每段 3–5 条要点，单条 ≤ 80 字，总计 ≤ 1200 字 |
| 事实约束 | 事实层（星曜落宫、四化、格局）由引擎结构化注入，Prompt 明令"不得编造盘面中不存在的星曜与格局"，输出后做一轮规则校验，幻觉星曜直接拦截并降级为规则解读 |
| 安全护栏 | 系统级拒答：医疗诊断、法律裁决、投资决策、孕产性别、生死吉凶、他人隐私查询。命中时返回预设引导话术 |
| 失败降级 | LLM 超时（> 20s）/ 报错 / 校验不通过 → 自动降级为**规则模板解读**，不显示错误页，并提示"当前使用基础解读" |
| 反馈闭环 | 每条要点可点「不准确」，累计反馈进入 Prompt 与规则迭代队列 |
| 成本控制 | 同一盘面结果缓存 30 天；免费额度每日 N 次（具体见 Q10） |
| 标识 | 结果区固定展示"AI 生成，仅供参考，不构成任何专业建议" |

**Prompt 分层结构**（实现约束，非最终文案）：

```
[system]  角色 + 流派设定（三合派/四化派）+ 输出 schema + 硬性禁令（禁止编造、禁止绝对化、禁止医疗法律金融建议）
[facts]   引擎注入的结构化盘面 JSON（唯一事实来源）
[style]   语气：克制、中性、非宿命论；禁用"一定/必然/注定/克死"等绝对化与恐吓性表述
[user]    用户提问（可选，限定在已授权范围内）
```

#### F7 流年与流月

| 项目 | 规格 |
| --- | --- |
| 流年盘构成 | 流年太岁宫（岁建）+ 流年四化（流年干）+ 流年斗君 + 流年诸星（流禄、流羊、流陀、流昌、流曲等） |
| 三层叠加 | 本命盘为底 → 叠加当前大限宫位 → 叠加流年宫位；命盘上分别用朱砂（大限）与金（流年）边框区分，可单独开关 |
| 时间选择 | 年份选择器（默认当前年，范围：出生年 ~ 当前年 + 30）；支持流月（12 宫按月顺行） |
| 输出 | 该年「重点宫位 Top 3」+ 白话解读 + 注意事项；不输出绝对化吉凶结论 |
| 边界 | 流年超出大限范围时提示"该年份超出当前大限推演范围，结果仅供参考" |

#### F9 合盘

| 项目 | 规格 |
| --- | --- |
| 输入 | 两张盘（可从盘库选择或新建），各标注角色（自己 / 对方）与关系类型 |
| 关系模式 | 情感婚姻 / 亲情亲子 / 事业合作 / 友谊，四种模式采用不同的宫位权重与解读模板 |
| 分析维度 | ① 命宫主星配对（性格互动）② 双方四化对对方命宫/夫妻宫的影响 ③ 关键宫位对应（如 A 的命宫落在 B 的何宫）④ 契合点与摩擦点各 3 条 |
| 输出 | 契合度可视化（**仅作示意，不给百分数字，避免误导**）+ 分维度文字解读 + 相处建议 |
| 隐私 | 合盘数据同样仅在本端计算；若调用 AI，两张盘均按 F6 脱敏规则处理，且需双方知情（提示"请确认已获得对方同意"） |
| 分享 | 合盘结果可生成分享图，默认隐藏双方姓名与具体生日 |

#### F8 格局库

- 规则以声明式 JSON 描述（`{ id, name, conditions: [{type, star, palace, brightness, mutual?}], source, description }`），新增格局无需改代码。
- 盘面命中格局后，在宫位详情与「格局」Tab 展示：成立条件（逐条列出命中的星曜与宫位）+ 说明 + 出处。
- 未完全成立但接近的组合，以「近似格局」弱化展示并说明差异，避免误判。

---

## 5. 核心业务规则：排盘引擎

> 排盘是**确定性计算**——相同输入必得相同命盘。引擎必须是纯函数、无副作用、可单测、规则数据驱动（规则以表/配置承载，不硬编码在逻辑里），以便多流派切换与后续校正。

### 5.1 引擎规则模块清单

| 模块 | 规则内容 | 流派差异点 | 备注 |
| --- | --- | --- | --- |
| M1 历法与时间 | 阳历↔农历转换、闰月、二十四节气（用于年/月的分界）、时区、真太阳时校正、早子/晚子换日 | 换日策略（晚子归当日 vs 次日） | 依赖历法库，需明确支持年份范围 |
| M2 命宫与身宫 | 寅宫起正月顺数至生月，再以该宫起子时逆数至生时 → 命宫；身宫按生时定位（子午/卯酉等规则依流派） | 身宫定位规则 | 五虎遁定十二宫天干 |
| M3 五行局 | 由命宫地支与生年天干查表得：水二 / 木三 / 金四 / 土五 / 火六局；局数决定起运年龄（2/3/4/5/6 岁起运）与紫微星起排 | 基本一致，少数派别有异 | 需提供"五行局推导过程"用于可解释性 |
| M4 紫微星系 | 由命宫地支 + 五行局定紫微落宫，再按固定间隔逆行安布紫微系诸星 | 基本一致 | 引擎需记录每一步落宫用于"依据链" |
| M5 天府星系 | 由紫微落宫推导天府落宫（寅申同宫、其余对称分列），再顺行安布天府系诸星 | 基本一致 | 同上 |
| M6 时系星 | 以生时定位：如文昌、文曲、地空、地劫、台辅、封诰等 | 部分星曜存在派别差异 | 可配置开关 |
| M7 月系星 | 以生月定位：如左辅、右弼、天刑、天姚、天巫、阴煞、解神等 | 部分星曜存在派别差异 | 可配置开关 |
| M8 年干系星 | 以生年天干定位：四化、禄存、擎羊、陀罗、天魁、天钺等 | **四化表存在流派差异（如庚干四化）** | 四化表配置化 |
| M9 年支系星 | 以生年地支定位：天马、红鸾、天喜、孤辰、寡宿、龙池凤阁等 | 部分星曜存在派别差异 | 可配置开关 |
| M10 十二宫排布 | 命宫起逆行安：兄弟、夫妻、子女、财帛、疾厄、迁移、交友、官禄、田宅、福德、父母 | 基本一致 | 每宫配天干（五虎遁） |
| M11 四化系统 | 生年四化（年干）→ 宫干四化 → 自化 → 飞星四化（四化派核心） | **流派差异最大** | 四化派（钦天门）需支持飞星路径与"忌冲"等推演 |
| M12 神煞杂曜 | 长生十二神、博士十二神、将前十二神、岁前十二神 | 基本一致 | P0 仅展示长生十二神 + 博士十二神，其余折叠 |
| M13 大运时限 | 大限：从命宫起，阳男阴女顺行、阴男阳女逆行，每宫十年，起点年龄 = 五行局数；小限：以生年支定位（顺逆规则依流派）；流年斗君、流月流日流时 | 小限顺逆、流月起点存在流派差异 | 需可配置 |
| M14 格局识别 | 规则化匹配星曜组合与宫位条件（如"三方四正会照""某星在某宫庙旺"） | 格局成立条件有流派出入 | 规则以声明式 JSON 描述，便于增补与勘误 |

### 5.2 庙旺利陷与星曜亮度

星曜在不同宫位有「庙、旺、得地、利益、平和、不得地、陷」七级亮度，直接影响格局判断与解读强度。P0 需内置亮度表并在星曜旁以小图标提示；P1 将亮度纳入格局成立条件与解读强度调节。

### 5.3 准确性保障方案

| 手段 | 说明 |
| --- | --- |
| **黄金样本集** | 建立 ≥ 200 个覆盖性样本（12 种命宫位置 × 5 种五行局、闰月、早子/晚子、跨时区、南北半球、性别各半），记录期望命盘快照 |
| **交叉验证** | 与成熟开源库（如 `iztro`）对同一输入做命盘 diff，差异项逐条人工裁定并归档为"已知差异（附理由）" |
| **CI 回归** | 引擎单测 + 快照测试纳入 CI，任何规则改动必须样本全绿或显式更新快照并说明 |
| **可解释输出** | 引擎除命盘外，另输出"推导轨迹"（每一步规则名 + 输入 + 输出），供可解释性与调试使用 |
| **勘误通道** | 用户可对任一宫位/结论提交"疑似有误"，进入人工复核队列 |

#### 黄金样本集构成明细

"200 个样本"必须按维度强制分布，否则无法覆盖边界：

| 维度 | 覆盖要求 |
| --- | --- |
| 命宫位置 | 12 地支各 ≥ 8 例 |
| 五行局 | 水二 / 木三 / 金四 / 土五 / 火六 各 ≥ 20 例 |
| 性别 | 男女各半（决定大限顺逆） |
| 历法 | 阳历 ≥ 100、农历 ≥ 80、其中闰月 ≥ 12 |
| 时辰 | 12 时辰全覆盖；早子、晚子各 ≥ 10 |
| 真太阳时 | 校正后**跨日或跨时辰**的样本 ≥ 20（最易出错） |
| 时区 | 非 UTC+8 出生 ≥ 15 |
| 年份跨度 | 1900–2100 均匀分布；边界年份各 ≥ 2 |
| 南北半球 | 各 ≥ 5 |

**样本维护规范**

- 存放于 `engine/__tests__/fixtures/`，每条含输入 + 期望命盘快照 + 来源 + 裁定人。
- 来源分三类：古籍案例、公开命例、人工构造的边界用例（需注明构造意图）。
- 新增或修改样本必须走 PR 评审，禁止单人直接改动快照。

### 5.4 输入边界与降级

- 年份超出历法库范围 → 明确拦截并说明原因，不静默计算。
- 「不知时辰」→ 提供两种降级：(a) 按午时排盘并在盘面全屏提示"时辰未定，命宫与迁移宫可能不准"；(b) 引导使用 P2 的定盘/问事流程。
- 出生地经纬度缺失（如海外小城市）→ 退化为"最近城市 + 提示误差分钟数"。

---

## 6. 视觉设计规范

### 6.1 设计风格

「新中式 · 宣纸书卷」风格：以宣纸米白为底、朱砂红为强调、墨色为文字主色，整体克制、典雅，去玄学低俗感。

**设计原则（命盘是信息密集界面，以下原则按优先级排序）**

1. **信息优先**：星曜、四化、宫位关系的可读性高于一切装饰。任何装饰元素不得遮挡或弱化星曜文字。
2. **克制**：不使用大面积高饱和色块、发光/霓虹、符咒式图案、闪烁动效。
3. **书卷气**：衬线/楷体字族、充分留白、用细线与留白分隔层级，而非用色块堆叠。
4. **一致性**：所有颜色、圆角、阴影、字号、间距统一走设计令牌（CSS Variables），**禁止组件内硬编码色值**。

### 6.2 配色方案

#### 6.2.1 浅色主题（宣纸，默认）

| 设计令牌 | 色值 | 用途 |
| --- | --- | --- |
| `--bg` | `#f5f0e6` | 页面背景（宣纸米白） |
| `--bg-card` | `#fdfaf3` | 卡片/面板背景（暖白） |
| `--ink` | `#2b2620` | 主文字（墨色） |
| `--ink-light` | `#6b6258` | 次要文字/说明（浅墨） |
| `--cinnabar` | `#a03a2b` | 主强调色（朱砂红） |
| `--cinnabar-dark` | `#842f22` | 深朱砂（按钮渐变/悬停） |
| `--gold` | `#b78a3f` | 点缀色（金色，标签/分隔线） |
| `--border` | `#e2d8c4` | 边框（浅米黄） |
| `--shadow` | `0 4px 20px rgba(60,45,25,0.06)` | 柔和阴影 |

渐变：

- 主按钮：`linear-gradient(135deg, #a03a2b, #842f22)`
- 顶部装饰线：`linear-gradient(90deg, #a03a2b, #b78a3f, #a03a2b)`
- 背景氛围：径向渐变朱砂/金色淡晕

#### 6.2.2 深色主题（书卷）

| 设计令牌 | 色值 | 用途 |
| --- | --- | --- |
| `--bg` | `#1e1a16` | 页面背景（深墨） |
| `--bg-card` | `#2a241d` | 卡片背景（深褐） |
| `--ink` | `#e8dfcf` | 主文字（米白） |
| `--ink-light` | `#b8ad9a` | 次要文字（灰米） |
| `--cinnabar` | `#d06a58` | 主强调色（亮朱砂） |
| `--cinnabar-dark` | `#b85344` | 深朱砂 |
| `--gold` | `#c9a35f` | 点缀色 |
| `--border` | `#42392c` | 边框 |
| `--shadow` | `0 4px 20px rgba(0,0,0,0.35)` | 阴影 |

#### 6.2.3 五行配色

| 五行 | 色值 | 视觉 |
| --- | --- | --- |
| 木 | `#4a7c59` | 青绿 |
| 火 | `#c0392b` | 赤红 |
| 土 | `#b7791f` | 棕黄 |
| 金 | `#8d8d8d` | 银灰 |
| 水 | `#2c3e50` | 深蓝 |

用途：五行局标识、宫位五行属性、星曜五行属性。

#### 6.2.4 旺衰五态标签配色

| 状态 | 文字色 | 背景 / 边框 |
| --- | --- | --- |
| 旺 `wang` | `#8a6522` | `rgba(183,138,63,0.12)` / `rgba(183,138,63,0.4)` |
| 相 `xiang` | `#3d6b4d` | `rgba(74,124,89,0.1)` / `rgba(74,124,89,0.3)` |
| 休 `xiu` | `#3d5f7a` | `rgba(74,110,140,0.1)` / `rgba(74,110,140,0.3)` |
| 囚 `qiu` | `#8a5a22` | `rgba(160,102,42,0.1)` / `rgba(160,102,42,0.3)` |
| 衰 `shuai` | `#6b6258` | `rgba(107,98,88,0.1)` / `rgba(107,98,88,0.3)` |

**与斗数庙旺七级的映射**（避免两套标签体系并存，统一复用五态色）：

| 斗数亮度（七级） | 复用状态 | 说明 |
| --- | --- | --- |
| 庙、旺 | 旺 | 星曜力量最强 |
| 得地、利益 | 相 | 力量得以发挥 |
| 平和 | 休 | 力量平稳 |
| 不得地 | 囚 | 力量受制 |
| 陷 | 衰 | 力量最弱 |

#### 6.2.5 四化配色（本产品补充，斗数核心）

用户提供的色板未含四化，按新中式色板补齐，全局语义固定：

| 四化 | 浅色主题 | 深色主题 | 语义 |
| --- | --- | --- | --- |
| 化禄 | `#b78a3f`（金） | `#c9a35f` | 资源、人缘、顺畅 |
| 化权 | `#a03a2b`（朱砂） | `#d06a58` | 掌控、竞争、主导 |
| 化科 | `#3d6b4d`（青） | `#5d9a72` | 名声、贵人、化解 |
| 化忌 | `#2b2620`（墨） | `#b8ad9a` | 执着、阻碍、纠结 |

四化在星曜上以**右上角小角标**呈现（12px，圆角 4px，半透明底 + 同色文字），不使用填充色块，以保持盘面克制。

#### 6.2.6 对比度校验结论

| 组合（浅色主题） | 实测对比度 | 结论 |
| --- | --- | --- |
| `--ink` on `--bg` | ≈ 11:1 | 通过 AAA，可用于任意字号 |
| `--ink-light` on `--bg` | ≈ 5.3:1 | 通过 AA，可用于正文与说明 |
| `--cinnabar` on `--bg` | ≈ 6.0:1 | 通过 AA，可用于链接与强调文本 |
| `--gold` on `--bg` | ≈ 2.8:1 | **不达标**，限定用于非文本元素（分隔线、边框、图标、装饰）或 ≥ 24px 加粗文本 |

### 6.3 字体规范

| 令牌 | 字体栈 | 用途 |
| --- | --- | --- |
| `--serif` | `"Songti SC","STSong","SimSun","Noto Serif SC",serif` | 正文、按钮、输入框 |
| `--kai` | `"Kaiti SC","STKaiti","KaiTi","Noto Serif SC",serif` | 标题、卦名、印章 |

**命盘专属字号（移动端基准，按 1dp 对应 1px 设计）**

| 元素 | 字族 | 字号 | 颜色 |
| --- | --- | --- | --- |
| 宫位名（命宫/财帛…） | `--kai` | 13px | `--ink` |
| 主星 | `--kai` | 16px | `--ink` |
| 辅曜 | `--serif` | 12px | `--ink-light` |
| 杂曜/神煞 | `--serif` | 11px | `--ink-light` |
| 宫位干支 | `--serif` | 11px | `--ink-light` |
| 大限年龄区间 | `--serif` | 11px | `--gold` |
| 中宫信息（命主/身主/五行局） | `--kai` | 13px | `--ink` |

兜底：Windows 无楷体时回退 `Noto Serif SC`，需引入 Web Font 子集（仅打包常用字，避免体积膨胀）。

### 6.4 核心视觉元素

| 元素 | 规格 |
| --- | --- |
| 印章 | 68px 朱砂红圆角方块 + 白色印文，居中于头部 |
| 六爻图 | 阳爻为实线（墨色 84px×8px）、阴爻为两段短线（中间断开）；动爻红底「动」标；移动端放大至 112px |
| 八卦符号 | Unicode 卦符（☰☱☲☳☴☵☶☷），34px |
| 卡片圆角 | `--radius: 14px` |
| 主按钮 | 朱砂红渐变 + 阴影，悬停上浮 |
| Tab/导航 | 朱砂红下划线标识当前项 |

**针对紫微斗数的适配说明**（六爻图为占卜类元素，斗数不使用，需限定用途）：

| 元素 | 在本产品中的定位 |
| --- | --- |
| 印章 | 品牌标识，置于页面头部与 PDF 报告页脚。印文建议用「紫」（紫微斗数标识）；若品牌名含「易」则沿用「易」 |
| 六爻图 | **仅作装饰**：用于空状态、加载占位、章节分隔；不作为功能控件 |
| 八卦符号 | **仅作装饰**：可用于命盘外围方位标注（十二地支方位）与术语词典配图，34px |
| 十二宫格 | 本产品的核心视觉，见 6.5 |

### 6.5 命盘组件视觉规格

**十二宫格**

- 单宫卡片：`--bg-card` 底 + 1px `--border` + `--radius` 圆角；宫格间距 6px。
- 当前大限宫位：1.5px `--cinnabar` 外框 + `rgba(160,58,43,0.06)` 淡底。
- 当前流年宫位：1.5px `--gold` 外框 + `rgba(183,138,63,0.08)` 淡底（与大限可叠加，用内框区分）。
- 身宫：金色虚线内框 + 右上角角标「身」。
- 命宫：`--cinnabar` 宫名（区别于其他宫位的 `--ink`），是盘面的视觉锚点。
- 中宫：`--bg-card` 底 + 朱砂/金径向淡晕背景，居中显示命主、身主、五行局、四柱、性别。
- 星曜数量超出宫格容量时，辅曜折叠为「+N」徽标，点击展开。

**其他组件**

| 组件 | 规格 |
| --- | --- |
| 顶部装饰线 | 朱砂→金→朱砂渐变，高 3px，贯穿页面顶部 |
| 盘面信息条 | 「已按真太阳时校正 +14 分钟」用 `--ink-light` 11px + ⓘ 图标，置于盘面上方 |
| 三层盘切换（本命/大限/流年） | 分段控件，选中项 `--cinnabar` 底 + 米白文字，切换动画 ≤ 300ms |
| Tab | 朱砂红 2px 下划线 + 当前项 `--ink` 加粗 |
| 主按钮 | 朱砂渐变 + `--shadow`，hover 上浮 2px，active 下压 1px，禁用态降饱和至 40% |
| 宫位详情抽屉 | 顶部圆角 16px，遮罩 `rgba(43,38,32,0.4)`，默认高度 70vh，可上滑至全屏 |
| 标签/徽标 | 圆角 6px，半透明底 + 同色文字（见 6.2.4 / 6.2.5） |
| 分隔 | 优先留白（16/24px），次选 1px `--border` 细线，不使用色块分隔 |

### 6.6 主题切换

- 默认浅色（宣纸），提供手动切换 + 首次访问跟随系统 `prefers-color-scheme`。
- 选择结果持久化到 localStorage，避免闪屏（首屏前内联注入主题脚本）。
- 切换过渡 200ms，仅过渡 `background-color / color / border-color`，禁止全局 `transition: all`。
- 所有颜色必须经 CSS 变量引用；命盘 SVG/Canvas 绘制部分同样从变量取值。

### 6.7 响应式与无障碍

- 断点：`≤ 640px` 移动 / `641–1024px` 平板 / `≥ 1025px` 桌面。
- 移动端宫格最小触控区 ≥ 44×44dp；宫格随屏幕宽度等比缩放，最小宽度不低于 72px。
- 对比度要求见 6.2.6；非文本内容（宫格外框、四化角标边框）对比度 ≥ 3:1。
- 支持系统字号缩放（`rem` 基准 + 允许 120% 放大不破版）。
- 尊重 `prefers-reduced-motion`：关闭宫格切换动画与上浮效果。
- 颜色不作为唯一信息载体：四化除颜色外必须带文字（禄/权/科/忌），亮度除颜色外带文字标签。

### 6.8 动效与微交互规范

**原则**：动效服务于"理解盘面变化"，不炫技。命盘是高频查看页面，任何动效都会累积成负担。

| 场景 | 动效 | 时长 / 曲线 |
| --- | --- | --- |
| 宫位点击 → 抽屉展开 | 抽屉上滑 + 遮罩淡入 | 280ms，`cubic-bezier(0.32, 0.72, 0, 1)` |
| 抽屉关闭 | 下滑 + 遮罩淡出 | 220ms |
| 大限 / 流年切换 | 相关宫位边框渐变 + 轻微缩放（1.0 → 1.03） | 300ms |
| 命盘首次渲染 | 十二宫格顺时针逐个淡入（stagger 30ms） | 总时长 ≤ 500ms |
| 主按钮 hover / active | 上浮 2px / 下压 1px | 160ms / 80ms |
| Tab 切换 | 下划线滑动 | 240ms |
| 主题切换 | 颜色过渡 | 200ms（见 6.6） |
| 排盘完成 | **无动效**（< 50ms） | — |
| 分享图生成 | 骨架屏 → 图片淡入 | 200ms |

**四条禁忌**

1. 禁止星曜闪烁、发光、旋转等玄学化动效——与"去低俗感"定位直接冲突。
2. 禁止超过 500ms 的过渡。
3. 禁止同时超过 3 个元素动画。
4. `prefers-reduced-motion: reduce` 时关闭全部动效，仅保留 `opacity` 变化。

**性能约束**：仅使用 `transform` 与 `opacity`，禁止动画 `width / height / top / left`（触发重排，移动端会掉帧）。

---

## 7. 页面结构与交互

### 7.1 页面清单

| 页面 | 路由 | 优先级 | 核心内容 |
| --- | --- | --- | --- |
| 首页 | `/` | P0 | 品牌 + 主排盘入口 + 最近盘 + 示例盘（名人/随机）+ 3 个引导卡片（准 / 懂 / 私密） |
| 排盘表单 | `/new` | P0 | 分步/单页表单 + 高级设置折叠 |
| 命盘主页 | `/chart/:id` | P0 | 十二宫格 + Tab（总览 / 十二宫 / 运势 / 格局） |
| 宫位详情 | 抽屉组件 | P0 | 见 F3 |
| AI 解读 | `/chart/:id/ai` | P1 | 授权页 + 流式解读结果 + 反馈 |
| 合盘 | `/synastry` | P1 | 双盘输入 + 关系分析 |
| 术语词典 | `/wiki/:category/:slug` | P1 | 词条详情 + 相关盘面跳转（路由规范见附录 D） |
| 我的盘库 | `/library` | P0 | 列表/卡片、搜索、标签筛选、删除 |
| 设置与关于 | `/settings` | P0 | 真太阳时默认、流派、隐私说明、免责声明、意见反馈 |

### 7.2 命盘页布局（移动端）

```
┌─────────────────────────────┐
│ ←  本命盘        [保存][分享] │
│ 已按真太阳时校正 +14 分钟  ⓘ │
│ [本命] [大限 ▾] [流年 2026]  │  ← 三层盘切换
├─────────────────────────────┤
│  ┌───┬───┬───┬───┐          │
│  │巳 │午 │未 │申 │          │
│  ├───┼───┼───┼───┤          │
│  │辰 │  中宫  │酉 │          │  ← 4×4 十二宫格
│  ├───┼───┼───┼───┤          │
│  │卯 │ 命主·身主 │戌 │          │
│  ├───┼───┼───┼───┤          │
│  │寅 │丑 │子 │亥 │          │
│  └───┴───┴───┴───┘          │
├─────────────────────────────┤
│ [总览] [十二宫] [运势] [格局] │  ← Tab
│                             │
│ 命宫：天同太阴 · 化禄         │
│ 你给人的第一印象是温和、好相处…│
│ ▸ 查看完整解读               │
└─────────────────────────────┘
```

**交互要点**：
- 宫格尺寸自适应，最小触控区 ≥ 44×44dp。
- 点击宫位 → 底部抽屉（高度 70vh，可上滑全屏），左右滑动切换宫位。
- 大限/流年切换时，相关宫位有过渡高亮动画（≤ 300ms）。
- 首屏只渲染本命盘，AI 解读、流年等按需懒加载。

### 7.3 状态设计

所有页面必须覆盖以下六种状态，**禁止只实现"理想态"**：

| 状态 | 触发场景 | 处理要求 |
| --- | --- | --- |
| 空状态 | 首次进入盘库、无格局命中、无历史 | 印章/八卦装饰图形 + 一句引导 + 主行动按钮。文案不说"暂无数据"，而给下一步动作（如"还没有命盘，立即排一张"） |
| 加载态 | 字体加载、AI 解读（8–15s） | 排盘 < 50ms 无需指示器；AI 解读用逐段流式出现 + 顶部进度 + 可中途取消。**禁用全屏转圈** |
| 错误态 | 历法越界、闰月不存在、网络异常、AI 失败 | 图标 + 具体原因 + 可操作补救。禁止"出错了，请重试"这类无信息量文案 |
| 边界态 | 不知时辰、出生地缺失、真太阳时导致跨日 | 不静默纠正；盘面顶部常驻提示条说明所采用的假设及影响范围 |
| 降级态 | AI 不可用、楷体缺失、浏览器不支持某特性 | 自动切换基础能力并明确告知；**核心排盘能力在任何降级下都不可用丧失** |
| 权限态 | 本地存储被禁用或已满 | 提示"无法保存到本地，是否仅查看"，不阻断浏览 |

**关键错误文案示例**（要求具体、可操作）：

| 场景 | 文案 |
| --- | --- |
| 历法越界 | 暂不支持 1899 年及以前的出生日期（历法数据范围为 1900–2100 年） |
| 闰月不存在 | 2026 年没有闰五月，请确认月份 |
| 不知时辰 | 未选择时辰将按午时排盘，命宫与迁移宫可能不准 · [去补充] |
| 真太阳时跨日 | 按真太阳时校正后，出生日期由 3 月 5 日变为 3 月 4 日 · [查看对比] |

### 7.4 关键交互流程

**流程 1：排盘主流程（含校验分支）**

```
输入 → 校验
        ├─ 历法越界 ────────→ 错误提示 + 建议范围
        ├─ 闰月不存在 ──────→ 错误提示 + 月份修正建议
        ├─ 时辰未知 ────────→ 提示条 + 按午时继续（可返回补充）
        └─ 通过
             └→ 真太阳时校正（若开启）
                  └→ 校正后跨日/跨时辰 → 二次确认弹窗（展示校正前后对比）
                        └→ 排盘 → 命盘页
```

**流程 2：AI 解读授权**

```
点击「AI 深度解读」
  → 授权弹窗
      将上传：命盘结构（星曜、四化、格局、大限流年）
      不会上传：姓名、备注、出生时间、设备标识
  → [同意并生成] → 流式生成 → 输出校验 ─┬─ 通过 → 展示（标注 AI 生成）
                                        └─ 命中幻觉/敏感内容 → 降级为规则解读
     [仅用基础解读] → 直接展示规则模板解读
```

**流程 3：分享与回流**

```
命盘页 [分享] → 生成链接（Base64 参数：日期/时辰/性别/时区/经度/策略，不含姓名备注）
  → 分享至社交平台
  → 接收者打开 → 直接还原命盘（无需注册）
       └→ 浮层「查看你自己的命盘」→ 进入排盘表单（核心转化点）
```

### 7.5 首屏与新手引导

紫微斗数对小白门槛高（十二宫、十四主星、四化均为陌生概念），需显式引导：

| 手段 | 规格 |
| --- | --- |
| 首页 | 3 个差异化卡片（准 / 懂 / 私密）+ 主 CTA + 示例盘入口 |
| 示例盘 | 提供 1–2 张预置示例盘，降低"必须输入生日才能进入"的门槛 |
| 首次进入命盘 | 三步气泡引导：① 这是你的命宫 ② 点宫位看解读 ③ 切换看今年运势。仅出现一次，可跳过，`localStorage` 记录 |
| 术语内联解释 | 术语首次出现带虚下划线，点击弹出解释卡（不跳页，避免打断阅读） |
| 免责声明 | 首次访问展示一次（可关闭）；AI 解读前再展示一次 |

### 7.6 桌面端与平板布局

第 7.2 节定义的是移动端形态，其余断点补充如下：

| 断点 | 布局 |
| --- | --- |
| ≤ 640px | 十二宫格全宽 4×4；宫位详情为**底部抽屉**（70vh，可上滑全屏） |
| 641–1024px | 宫格居中，最大宽度 720px；详情为**右侧抽屉**（420px） |
| ≥ 1025px | **三栏布局**：左 240px（盘库/切换盘）· 中 命盘（最大 640px）· 右 400px 常驻详情面板 |

**桌面端增强**（不移植到移动端）

- 右侧详情面板常驻，点击宫位即切换内容，**省去一次开关交互**。
- 键盘导航：方向键在宫位间移动，`Enter` 打开详情，`Esc` 关闭。
- 星曜悬浮显示 tooltip 速览（不点击也能看到基本信息）。
- `Ctrl/Cmd + F` 页内搜索星曜并高亮所在宫位。

**共同约束**

- 命盘为正方形，**最大宽度锁定 640px**——超出会破坏宫格比例，导致星曜挤压。
- 深浅色主题、字号、设计令牌在所有断点完全一致。
- 桌面端不做独立信息架构，与移动端共用同一套路由与组件。

---

## 8. 非功能需求

| 类别 | 要求 |
| --- | --- |
| **性能** | 排盘计算 < 50ms；首屏 JS ≤ 200KB（gzip）；LCP < 2.5s（4G 中端机）；命盘渲染无掉帧（宫格组件 memo 化） |
| **隐私** | 默认纯前端：出生数据仅存本地，服务端无日志；AI 解读需二次确认，仅传结构化盘面 JSON（不含姓名/备注/IP 关联画像）；提供"一键清除本地数据" |
| **准确性** | 见 5.3；任何规则变更需走样本回归 |
| **合规** | 全局娱乐/文化研究定位；首屏可关闭的免责浮层；健康/医疗/法律/投资类问题由 LLM 与文案双重拦截；未成年人保护提示；不承诺任何"效果" |
| **可用性** | 核心流程 3 步内完成；关键错误（历法越界、闰月不存在）提示具体可操作 |
| **无障碍** | WCAG 2.1 AA：对比度 ≥ 4.5:1、支持系统字号缩放、宫格可键盘导航、动效可关闭、颜色不作为唯一信息载体（详见 6.7） |
| **兼容性** | iOS Safari 15+、Android Chrome 90+、微信内置浏览器、桌面 Chrome/Edge/Safari；响应式 320–1440px |
| **可观测** | 前端埋点（页面 PV、排盘完成、宫位点击、分享、AI 解读发起/完成），**不含**出生时间等敏感原始值 |
| **国际化** | 代码与文案分离（i18n key），P0 仅简体中文，结构上预留繁体/英文 |

---

## 9. 技术方案

### 9.1 总体架构

```
┌──────────────────────── 浏览器（纯前端） ────────────────────────┐
│  UI 层（React）                                                 │
│   命盘渲染 / 宫位详情 / 解读 / 运势 / 合盘 / 盘库                  │
│                          ↓ 纯函数调用                            │
│  排盘引擎（TypeScript，零 UI 依赖，可独立发包）                    │
│   ├─ calendar/  历法转换 · 时区 · 真太阳时                        │
│   ├─ rules/     规则表（星曜/四化/五行局/神煞/格局，声明式数据）    │
│   ├─ core/      命宫定位 · 安星 · 四化 · 大限流年                  │
│   └─ types.ts   领域模型 + 推导轨迹（trace）                      │
│                          ↓                                      │
│   本地存储：localStorage / IndexedDB                             │
└──────────────────────────┬──────────────────────────────────────┘
                           │ 仅 AI 解读（用户授权，脱敏盘面 JSON）
                           ▼
              Edge Function（Serverless）→ LLM 流式输出
```

**核心原则**：排盘 100% 在客户端完成 → 零后端成本、数据不出端、可离线（PWA 后）。

### 9.2 技术选型

| 层 | 选型 | 理由 |
| --- | --- | --- |
| 框架 | React 18 + TypeScript | 生态成熟、类型安全（排盘规则的类型约束至关重要） |
| **构建与渲染** | **Vite + SSG 预渲染（词条页/首页静态化 + 排盘页 SPA）**，或 Next.js App Router | **SEO 硬需求**：纯 SPA 会导致搜索引擎抓不到词条页正文，直接影响 40–50% 的自然流量。需在 M0 决策（Q11）。内容页静态化，交互页走客户端渲染，两者共用同一套 UI 组件与引擎 |
| 样式 | Tailwind CSS + CSS Variables（设计令牌见第 6 章与附录 C） | 快速实现响应式与主题；色值/圆角/阴影统一由 tokens 承载，禁止组件内硬编码 |
| 状态 | Zustand | 轻量，适合"单张当前命盘 + 盘库"的状态规模 |
| 历法 | `lunar-typescript`（农历/干支/节气/闰月） | 成熟、覆盖 1900-2100；备选 `tyme4ts` |
| 排盘引擎 | **MVP 阶段**：基于 `iztro` 快速验证；**V1.0**：自研核心 | `iztro` 成熟但流派可配置性与"推导轨迹"能力不足，无法满足可解释性与多流派诉求；MVP 用其做交叉验证与加速冷启动 |
| 测试 | Vitest（引擎单测 + 200 黄金样本快照）+ Playwright（E2E 冒烟） | 准确性是生命线 |
| 组件文档 | Storybook（宫格组件） | 星曜组合状态多，需可视化回归 |
| 部署 | 静态托管：Vercel / Cloudflare Pages / Netlify | 纯静态，全球 CDN |
| AI（P1） | Edge Function 代理 LLM，模板化 Prompt，流式返回 | 不暴露密钥；统一做敏感话题拦截与输出结构化 |

**引擎自研决策说明**：借 `iztro` 起步能省 2–3 周，但本产品差异点是"可解释 + 多流派"，二者都要求规则可插拔与推导过程可输出，第三方库难以满足。因此 MVP 双轨（自研引擎跑样本 + iztro 交叉校验），V1.0 切换到自研引擎。

### 9.3 核心数据模型（TypeScript 草案）

```ts
// 输入
interface BirthInput {
  calendar: 'solar' | 'lunar';
  solarDate: string;        // ISO date
  lunarDate?: { year: number; month: number; day: number; isLeapMonth: boolean };
  timeIndex: number;        // 0–11，十二时辰
  isEarlyZi?: boolean;      // 早子时（00–01）
  gender: 'male' | 'female';
  timezone: string;         // IANA，如 Asia/Shanghai
  longitude?: number;       // 出生地经度，用于真太阳时
  useTrueSolarTime: boolean;
  ziStrategy: 'late-zi-same-day' | 'late-zi-next-day';
}

// 单宫
interface Palace {
  index: number;                 // 0–11
  name: PalaceName;              // 命宫/兄弟/...
  heavenlyStem: string;
  earthlyBranch: string;
  isBodyPalace: boolean;
  majorStars: Star[];            // 十四主星
  minorStars: Star[];            // 辅曜
  adjectiveStars: Star[];        // 杂曜/神煞
  changSheng?: string;           // 长生十二神
  daXian?: { start: number; end: number }; // 大限年龄区间
  xiaoXian?: number;
}

interface Star {
  name: string;
  type: 'major' | 'minor' | 'adjective';
  brightness?: '庙' | '旺' | '得地' | '利益' | '平和' | '不得地' | '陷';
  mutagen?: '禄' | '权' | '科' | '忌';   // 四化
  mutagenSource?: 'birth-year' | 'palace-stem' | 'self';
}

// 命盘（含推导轨迹，供可解释性使用）
interface Chart {
  input: BirthInput;
  soulPalaceIndex: number;
  bodyPalaceIndex: number;
  fiveElementsClass: '水二局' | '木三局' | '金四局' | '土五局' | '火六局';
  palaces: Palace[];
  horoscope: { daXian: DaXian[]; xiaoXian: number[]; liuNian: Record<number, LiuNian> };
  patterns: Pattern[];
  trace: TraceStep[];   // 每一步规则推导：{ rule, inputs, output }
}
```

### 9.4 目录结构

```
src/
├─ engine/                 # 排盘引擎（纯 TS，可独立发布为 npm 包）
│  ├─ calendar/            # 历法、时区、真太阳时
│  ├─ rules/               # 规则数据（星曜表、四化表、五行局表、神煞表、格局规则）
│  ├─ core/                # 命宫定位、安星、四化、大限流年、格局匹配
│  ├─ __tests__/           # 单测 + 黄金样本
│  └─ types.ts
├─ data/                   # 内容：星曜释义、宫位释义、格局文案、术语词典
├─ features/
│  ├─ chart/               # 命盘渲染（PalaceGrid、PalaceDetail）
│  ├─ reading/             # 基础解读 + AI 解读
│  ├─ horoscope/           # 大限 / 流年 / 流月
│  ├─ synastry/            # 合盘
│  └─ library/             # 盘库与分享
├─ components/             # 通用组件
├─ store/                  # Zustand
├─ i18n/
└─ pages/
```

### 9.5 AI 解读接口契约

边缘函数 `POST /api/reading`，仅此一个服务端接口，其余能力全部在客户端完成。

**请求**

```ts
interface ReadingRequest {
  chartFacts: {          // 由引擎生成的结构化事实层，服务端不做任何命理计算
    fiveElementsClass: string;
    soulPalace: number;
    bodyPalace: number;
    palaces: Array<{ name: string; majorStars: string[]; brightness?: string[]; mutagen?: string[] }>;
    patterns: string[];
    daXian: { current: number; range: [number, number] };
    liuNian: number;
  };
  school: 'san-he' | 'si-hua';   // 流派
  locale: 'zh-CN';
  question?: string;             // 用户提问，长度 ≤ 50 字，前端做敏感词初筛
}
```

**响应**：SSE 流式（`text/event-stream`），事件类型 `delta` / `done` / `error`。

| 错误码 | 含义 | 前端处理 |
| --- | --- | --- |
| `400 INVALID_INPUT` | 盘面结构不合法 | 记录日志，降级为规则解读 |
| `403 BLOCKED` | 命中敏感话题 | 展示预设引导话术，不计入失败率 |
| `429 RATE_LIMIT` | 超出额度 | 展示剩余额度与恢复时间，引导使用基础解读 |
| `504 TIMEOUT` | 超过 20s | 自动降级为规则解读，不展示错误页 |

**服务端约束**：请求日志仅保留 30 天且**不记录 `chartFacts` 明细**（出生时间不可反推）；不落库任何可关联到个人的数据；按 IP + 匿名 ID 限流。

### 9.6 存储与数据生命周期

| 数据 | 位置 | 生命周期 | 说明 |
| --- | --- | --- | --- |
| 主题、引导标记、免责已读 | `localStorage` | 长期，用户可清 | 体积极小（< 2KB） |
| 命盘库（输入参数 + 备注标签） | `IndexedDB` | 长期，用户可清 | 存**排盘输入参数**而非盘面结果，避免规则升级后旧盘不更新 |
| 分享链接 | URL 参数 | 一次性 | Base64 编码，不含姓名/备注 |
| AI 解读结果 | 内存 + 可选 IndexedDB 缓存 | 30 天 | 同一盘面复用，降低成本 |
| 服务端请求日志 | Edge 日志 | 30 天 | 不含 `chartFacts` 明细 |

**清理能力**：设置页提供「清除本地全部数据」，一键删除 IndexedDB 与 localStorage，二次确认后执行。

### 9.7 状态管理与性能策略

- **Store 划分**：`useChartStore`（当前盘 + 派生数据）、`useLibraryStore`（盘库）、`useUIStore`（主题、抽屉、Tab）。盘面对象用 `useMemo` 派生，避免每次渲染重算。
- **渲染优化**：十二宫格组件 `React.memo` + 稳定 `key`；宫位详情抽屉懒加载；AI 解读、流年、合盘按路由级 `React.lazy` 分包。
- **首屏**：内联关键 CSS 与主题脚本；Web 字体 `font-display: swap`；楷体仅打包常用字子集（目标 ≤ 120KB）。
- **包体预算**：首屏 JS ≤ 200KB（gzip）；引擎包 ≤ 60KB；内容文案按需分包。
- **缓存**：静态资源 `Cache-First` + 版本化文件名；排盘为纯计算无需缓存；PWA（P2）阶段再引入 Service Worker。

### 9.8 排盘引擎 API 设计

引擎作为独立包 `@ziwei/engine` 发布（便于测试、复用与未来开源），对外仅暴露四个入口：

```ts
// 1. 排盘（核心）
function astrolabeByBirth(input: BirthInput): Result<Chart>;

// 2. 运势推演（大限 / 流年 / 流月）
function horoscope(chart: Chart, target: { year: number; month?: number }): Result<HoroscopeView>;

// 3. 格局识别
function detectPatterns(chart: Chart, opts?: { school: School }): Pattern[];

// 4. 可解释性与词典（供依据链、SEO 词条页复用）
function explain(star: string): StarMeta | undefined;
function traceOf(chart: Chart): TraceStep[];
```

**设计约束**

- **纯函数**：无 I/O、无全局状态、无随机、无 `Date.now()`（时间全部由输入决定）——保证相同输入永远得到相同输出，这也是可做快照测试的前提。
- `BirthInput` 完全可序列化，便于分享链接与持久化。
- 规则全部从 `rules/` 读取并支持 `config` 覆盖；流派差异通过**配置切换**而非分支代码。
- **错误以 `Result` 返回，不抛异常**，UI 层可穷举处理全部边界：

```ts
type Result<T> = { ok: true; value: T } | { ok: false; error: EngineError };

type EngineError = {
  code: 'DATE_OUT_OF_RANGE' | 'LEAP_MONTH_INVALID' | 'INVALID_TIME_INDEX' | 'UNKNOWN_LOCATION';
  message: string;    // 面向用户的中文提示，可直接展示
  detail?: unknown;   // 调试信息
};
```

- 引擎不依赖任何 UI 框架与浏览器 API，可在 Node（CI 测试）、浏览器、Web Worker 中运行。

### 9.9 测试策略与 CI/CD

准确性是本产品的生命线，测试策略围绕它构建：

| 层 | 工具 | 覆盖内容 | 通过标准 |
| --- | --- | --- | --- |
| 单元测试 | Vitest | 单条安星规则、四化表、五行局推导、历法换算 | 引擎行覆盖率 ≥ 90% |
| **回归测试** | Vitest snapshot | 200+ 黄金样本的全盘快照 | **100% 匹配；变更需显式更新并说明理由** |
| 交叉验证 | 自定义脚本 | 与 `iztro` 对同一输入做命盘 diff | 差异项全部有裁定结论并归档 |
| E2E | Playwright | 排盘主流程、宫位交互、分享还原 | 冒烟用例 100% 通过 |
| 性能 | Lighthouse CI | 首屏、LCP、包体 | Performance ≥ 85 |

**CI 流水线**

```
push / PR
  → lint + typecheck
  → 单元测试 + 引擎覆盖率
  → 黄金样本回归
  → 交叉验证 diff 报告
  → 构建 + 包体预算检查
  → E2E 冒烟
  → Lighthouse CI
  → 预览环境部署
```

**卡点规则**

| 检查结果 | 处理 |
| --- | --- |
| 黄金样本不匹配 | **阻断合并** |
| 引擎覆盖率 < 90% | 警告，不阻断 |
| Lighthouse Performance < 85（main 分支） | **阻断** |
| 包体超预算 | **阻断** |
| 扫描到硬编码色值（tokens 文件外） | **阻断** |

**线上监控（P1）**

- 前端异常监控（Sentry）：引擎运行时异常采样率 100%——引擎异常必须全量可见，不允许被采样丢弃。
- 关键告警：排盘错误率 > 2%、AI 降级率 > 15% 时告警（与第 12 章埋点体系打通）。

---

## 10. 内容与数据

| 内容资产 | 规模 | 说明 |
| --- | --- | --- |
| 星曜释义 | 14 主星 + 18 辅曜 + 杂曜 | 每颗：基本性质、入十二宫简述、四化后的变化、庙旺陷差异 |
| 宫位释义 | 12 宫 | 宫位含义、代表的人事物、强弱判断要点 |
| 主星组合 | 约 30+ 双星组合（P0 覆盖高频 12 组） | 双星同宫的组合解读 |
| 格局库 | P0：20 条；P1：60+ 条 | 成立条件（声明式）+ 说明 + 出处 |
| 术语词典 | 150+ 词条 | 支持从盘面任意星曜/宫位跳转 |
| 免责与合规文案 | 全局 | 娱乐定位、非医疗法律金融建议、未成年人提示 |

**内容规范**：
- 结论不得绝对化（禁用"一定""必然""必死"等），统一使用"倾向/更容易/需要注意"等表述。
- 涉及健康的描述统一为养生提示，并附"如有不适请咨询专业医师"。
- 所有 AI 生成内容需在前端标注"AI 生成，仅供参考"。

### 10.1 内容生产流程与审校机制

R9 已指出内容是被低估的工作量，需以流程保障而非靠人力硬扛：

| 内容 | 规模 | 生产者 | 审校要求 |
| --- | --- | --- | --- |
| 星曜释义（14 主星 + 18 辅曜） | 32 条 | AI 初稿 + 命理顾问 | **100% 审校** |
| 双星组合解读 | 30+ 条 | AI 初稿 | 抽检 30% |
| 宫位释义 | 12 条 | 人工撰写 | **100% 审校** |
| 格局库说明 | 60+ 条 | 规则化生成 + 人工补说明 | 抽检 50% |
| 术语词典 | 150+ 条 | AI 初稿 | 抽检 20% |

**流程**

```
规则确定 → AI 按模板批量生成 → 命理顾问审校 → 结构化入库（JSON，含版本号）
        → 前端按 key 引用 → 用户勘误回流 → 修订并递增版本
```

- 内容以 JSON 入库并版本化，**文案修订无需重新发版**（支持热更新）。
- 每条内容必须可追溯到"依据"（引用的星曜 / 宫位 / 四化），无依据的文案不予入库。
- 用户勘误（见 5.3 勘误通道）进入内容修订队列，形成闭环。

**字数规范**：星曜释义 200–400 字；宫位解读 150–300 字；格局说明 100–200 字；术语词条 80–150 字。

---

## 11. 增长与获客

### 11.1 渠道判断

命理工具类站点的典型流量结构：搜索 40–50%、社交分享 30–40%、直接访问与口碑 15–20%。据此分配投入：

| 渠道 | 优先级 | 关键动作 | 衡量指标 |
| --- | --- | --- | --- |
| **SEO（长尾搜索）** | P0 | 见 11.2 | 收录量、自然搜索点击、词条页 PV |
| **社交分享** | P0 | 见 11.3 | 分享率、分享回流转化率 |
| 内容矩阵 | P1 | 见 11.4 | 长尾页 PV、词条页跳出率 |
| 社群口碑 | P2 | 爱好者社区自然渗透 | 品牌词搜索量 |

### 11.2 SEO 策略（重点）

命理是典型的**长尾搜索市场**：用户搜索词高度分散（"紫微斗数排盘""天同星在命宫是什么意思""紫微斗数免费排盘"），单靠首页无法覆盖。

**页面矩阵**

| 层级 | 路由示例 | 数量 | SEO 价值 |
| --- | --- | --- | --- |
| 工具页 | `/` | 1 | 高竞争词，难排名 |
| **星曜词条页** | `/wiki/star/tian-tong` | 30+ | **长尾主力，易排名** |
| 双星组合页 | `/wiki/combo/tong-yin` | 30+ | 长尾，竞争小 |
| 格局页 | `/wiki/pattern/zi-fu-chao-yuan` | 60+ | 长尾 |
| 宫位释义页 | `/wiki/palace/cai-bo` | 12 | 中 |

**技术硬约束（必须在 M0 决策，见 Q11）**

- 词条页必须**静态化**（SSG / 预渲染）；纯 SPA 会导致搜索引擎抓不到内容，SEO 全废。
- 结构化数据：`FAQPage` / `Article` schema。
- 每页独立 `title` / `description`（模板化生成，禁止全站共用一段）。
- `sitemap.xml`、`robots.txt`、语义化 slug、`canonical`。
- 首屏内容服务端可见（与 LCP 优化目标一致，不冲突）。

### 11.3 分享与传播机制

命盘本身是天然传播载体，分享卡片设计要点：

| 项目 | 规格 |
| --- | --- |
| 形态 | 生成图片（社交平台对链接不友好），尺寸 750×1000 |
| 内容 | 命盘缩略图 + 命宫主星大字 + 一句钩子文案 + 品牌印章 |
| 钩子文案 | 具体、可验证、不涉隐私。如「我的命宫是天同太阴，据说天生好人缘」「这张盘校正了 14 分钟真太阳时」 |
| 禁止包含 | 姓名、具体出生日期、出生地点 |
| 回流设计 | 分享页打开展示完整命盘（无需注册）+ 浮层「看看你自己的命盘」→ 进入排盘表单 |
| 追踪 | 链接带 `utm` 参数，区分渠道 |

### 11.4 内容矩阵（P1）

- 星曜词条页（SEO 主力，与术语词典复用同一份内容资产）
- 「一分钟看懂你的命盘」引导内容
- 常见误解澄清（如「化忌不等于凶」「命盘不是宿命」）——兼具 SEO 价值与品牌调性塑造

### 11.5 留存与召回

- **盘库即留存**：用户为家人/朋友排盘后，多张盘构成回访理由。
- 流年更新提醒（P2，需授权）：每年立春推送「你的 2027 年运势已更新」。
- 无账号体系下，召回依赖：PWA 安装、浏览器书签、社交回流。

### 11.6 商业化路径（P2，需数据验证后再启动）

优先级由低风险到高风险：① 捐赠/赞赏 → ② 高级报告付费（一次性）→ ③ 订阅制（无限 AI 解读 + 多流派 + 从业者工作台）。

**红线**：不售卖改运/法事/开运商品，不做按结果收费的咨询服务，不在命盘中投放与命理相关的广告。

### 11.7 冷启动策略

SEO 见效需要 3–6 个月，必须为零流量期设计启动手段：

| 阶段 | 时间 | 动作 | 目标 |
| --- | --- | --- | --- |
| **T0 上线首月** | 0–4 周 | ① 提交 sitemap 至百度/谷歌站长平台 ② 在 3–5 个爱好者社群首发（豆瓣小组、小红书、贴吧），卖点聚焦"排得准 + 讲得清" ③ 邀请命理顾问背书 | 日均 50–100 访客 |
| **T1 内容爬坡** | 1–3 月 | 每周上线 8–10 个词条页，累计 100+ | 长尾词开始收录 |
| **T2 口碑期** | 3–6 月 | 依赖分享回流 + 搜索自然增长 | 日均 1,000+ |

**冷启动核心钩子**：发布《我们如何验证排盘准确性》技术文章，公开黄金样本集与和 `iztro` 的 diff 报告。

这是竞品集体缺失、而爱好者群体最在意的东西——用"可验证的准确"建立专业信任，比任何功能宣传都有效，且内容本身具备长尾搜索价值。

---

## 12. 数据埋点与指标体系

### 12.1 埋点原则

- **敏感数据不埋**：出生年月日时、经纬度、姓名、备注禁止进入任何埋点事件。
- 只允许采集盘面**类别特征**（五行局、命宫主星名、命宫地支）——这些不构成个人标识。
- 统一封装 `track(event, props)`，禁止散落调用，便于统一审计与关闭。

### 12.2 核心事件表

| 事件 | 触发时机 | 关键属性 |
| --- | --- | --- |
| `page_view` | 页面进入 | `page`, `referrer`, `utm_source` |
| `chart_input_start` | 进入排盘表单 | — |
| `chart_submit` | 点击「开始排盘」 | `calendar`, `has_birthplace`, `use_true_solar_time`, `zi_strategy` |
| `chart_success` | 命盘渲染完成 | `duration_ms`, `five_elements_class`, `soul_palace_star` |
| `chart_error` | 排盘失败 | `error_type`（历法越界 / 闰月 / 其他） |
| `palace_click` | 点击宫位 | `palace_name` |
| `star_wiki_open` | 打开术语解释 | `star_name` |
| `da_xian_switch` / `liu_nian_switch` | 切换大限 / 流年 | `year` |
| `ai_reading_request` / `_done` / `_fail` | AI 解读各环节 | `duration_ms`, `fail_reason` |
| `ai_reading_fallback` | 降级为规则解读 | `reason` |
| `share_click` | 点击分享 | `share_type`（链接 / 图片） |
| `chart_save` | 保存到盘库 | — |
| `disclaimer_view` | 免责声明曝光 | `position` |

### 12.3 指标体系

**北极星指标**：周活跃排盘用户（WAU）

| 层级 | 指标 | 定义 | 目标 |
| --- | --- | --- | --- |
| 获取 | 自然搜索点击量 | Search Console | 上线 3 个月 ≥ 3,000/月 |
| 激活 | **排盘完成率** | `chart_success` / `chart_input_start` | ≥ 75% |
| 激活 | 排盘错误率 | `chart_error` / `chart_submit` | ≤ 2% |
| 参与 | **宫位探索深度** | 人均 `palace_click` | ≥ 4 次/盘 |
| 参与 | 术语打开率 | 有 `star_wiki_open` 的会话占比 | ≥ 30% |
| 传播 | **分享率** | `share_click` / `chart_success` | ≥ 15% |
| 传播 | 分享回流转化 | 分享页进入并完成排盘 / 分享页访问 | ≥ 20% |
| 深度 | AI 解读完成率 | `_done` / `_request` | ≥ 90% |
| 深度 | **AI 降级率** | `_fallback` / `_request` | ≤ 5% |
| 留存 | 7 日留存 | — | ≥ 10% |

### 12.4 数据使用约束

- 不使用会采集可反推个人身份信息的第三方统计 SDK。
- 埋点数据不用于广告定向投放。
- 设置页提供「拒绝匿名统计」开关，关闭后仅保留必要的性能监控。

---

## 13. 里程碑与排期

| 阶段 | 周期 | 交付物 | 出口标准 |
| --- | --- | --- | --- |
### 13.1 里程碑

| 阶段 | 周期 | 交付物 | 出口标准 |
| --- | --- | --- | --- |
| **M0 需求、规则与技术选型** | 1 周 | 规则表（星曜/四化/五行局）、50 个黄金样本、原型稿、**SSG 方案选型（Q11）** | 规则表评审通过；样本集可运行；渲染方案确定 |
| **M1 引擎与盘面（Alpha）** | 3 周 | 排盘引擎 + 十二宫格渲染 + 宫位详情 + 首页/词条页静态化骨架 | 50 → 200 样本全绿；与 iztro 交叉验证差异已裁定 |
| **M2 解读与运势（Beta）** | 2 周 | 基础解读文案、大限流年、盘库与分享、导出图、**埋点体系** | 内测 20 人可用性通过；Lighthouse ≥ 85；核心漏斗可观测 |
| **M3 正式版 v1.0** | 2 周 | AI 解读、合盘、格局库、术语词典、**星曜词条页 30+ 上线** | 合规审核通过；Sitemap 提交；公测发布 |
| **M4+ 迭代** | 持续 | 多流派、从业者工作台、多语言、PWA | 依据数据决策 |

**关键路径**：规则表梳理（M0）→ 引擎准确性（M1）→ 内容资产（M2/M3）。

其中**内容资产是最容易被低估的工作量**（星曜释义 + 30 双星组合 + 60 格局 + 12 宫位 ≈ 数百条文案），且直接决定 M3 的 SEO 上线，需从 M1 起并行启动，不可串行等待。

### 13.2 发布与灰度策略

| 阶段 | 范围 | 策略 | 回滚条件 |
| --- | --- | --- | --- |
| Alpha | 内部 + 5 位命理顾问 | 手工分发，重点验证排盘准确性 | — |
| Beta | 100 人内测（爱好者社群招募） | 开放链接，收集排盘勘误与可用性问题 | — |
| 公测 v1.0 | 全量 | 功能开关控制 AI 解读、合盘、流月 | 单功能可独立关闭，不影响排盘主链路 |
| 灰度放量 | — | 新功能按 5% → 20% → 100% 三档，每档观察 ≥ 24h | 错误率 > 5% 或 AI 降级率 > 15% 立即回滚 |

**Feature Flag 清单**：`ai_reading`、`synastry`、`liu_yue`、`multi_school`、`export_pdf`。

### 13.3 关键依赖与前置条件

| 依赖项 | 责任方 | 起始 | 阻塞内容 |
| --- | --- | --- | --- |
| 命理顾问（规则审定、流派差异裁定） | 外部 | M0 | M1 引擎验收 |
| 内容文案撰写（星曜/宫位/组合/格局） | 内容 | M1 | M2 解读、M3 词条页 |
| LLM API 额度与合规审核 | 技术/法务 | M2 | M3 AI 解读 |
| SSG 方案与托管平台选定 | 技术 | M0 | M3 SEO 上线 |

---

## 14. 风险与对策

| # | 风险 | 影响 | 概率 | 对策 |
| --- | --- | --- | --- | --- |
| R1 | 排盘规则流派分歧导致"不准确"争议 | 高（核心可信度） | 高 | 规则数据化 + 流派明示 + 依据链下钻 + 勘误通道；争议项以"主流做法 + 标注差异"呈现 |
| R2 | AI 解读产生幻觉或不当建议 | 高（合规） | 中 | 结构化输入（不自由发挥盘面事实）+ 模板化 Prompt + 输出 schema 约束 + 敏感话题拒答 + 结果免责 + 用户反馈闭环 |
| R3 | 命理类内容的平台/合规风险 | 高 | 中 | 娱乐与文化研究定位、全局免责、不承诺效果、不涉医疗/法律/金融、不做交易型业务 |
| R4 | 历法库年份边界与精度 | 中 | 低 | 明确 1900–2100 支持范围，越界拦截并提示；真太阳时误差 > 5 分钟时提示 |
| R5 | 与现有工具同质化、缺乏传播 | 中 | 中 | 押注"可解释性"差异化；分享卡片突出"校正了 X 分钟""识别到 N 个格局"等具体钩子 |
| R6 | 内容资产工作量大（星曜/组合/格局文案） | 中 | 高 | P0 聚焦高频 20%；文案结构化复用（星曜释义 × 宫位模板），AI 辅助生成 + 人工审校 |
| R7 | 依赖第三方库（iztro / lunar-typescript）停更或与预期不符 | 中 | 中 | 引擎抽象层隔离第三方；V1.0 自研核心；历法库做薄封装便于替换 |
| R8 | **技术选型失误导致 SEO 全废**（纯 SPA 无法被搜索引擎抓取） | 高 | 中 | M0 阶段强制决策（Q11）；词条页必须 SSG/预渲染；上线前用抓取工具验证首屏可见内容 |
| R9 | **内容资产工作量超预期**，拖累 M3 上线 | 中 | **高** | M1 起并行启动；P0 只覆盖高频 20%；文案结构化复用 + AI 辅助生成 + 人工审校；按词条页优先级分批上线 |
| R10 | AI 调用成本随用户量线性上涨 | 中 | 中 | 结果缓存 30 天；免费额度限制；规则解读兜底；预留切换更廉价模型的能力 |
| R11 | 埋点或分享链接意外泄露出生信息 | 高 | 低 | 埋点禁止采集原始出生数据（12.1 硬约束）；分享链接仅含排盘参数；上线前做一次数据流审计 |

---

## 15. 验收标准

### 15.1 引擎验收

- [ ] 200+ 黄金样本 100% 匹配快照，CI 强制通过。
- [ ] 与 `iztro` 交叉验证：差异项 100% 有人工裁定结论并归档。
- [ ] 覆盖边界用例：闰月、早子/晚子（两种策略）、跨时区出生、真太阳时导致日期变更、南北半球、1900/2100 边界。
- [ ] 单测覆盖率：引擎 ≥ 90%。

### 15.2 产品验收

- [ ] 首次访问 → 看到命盘 ≤ 3 步、≤ 30 秒（10 人可用性测试，8 人通过）。
- [ ] 移动端 Lighthouse Performance ≥ 85，LCP < 2.5s；排盘计算 < 50ms。
- [ ] 12 宫位、14 主星、18 辅曜释义齐全；点击星曜可跳转词典。
- [ ] 真太阳时校正结果在盘面明示，且可一键关闭/开启对比。
- [ ] 分享链接可完整还原命盘，且链接参数中不含姓名/备注。
- [ ] 无后端依赖时，断网仍可排盘（P0 静态资源已缓存即可）。
- [ ] 免责声明在首次访问与 AI 解读前各出现一次。

### 15.3 视觉验收

- [ ] 全部颜色经设计令牌引用，代码扫描无硬编码色值（`--` 变量外的 hex 仅允许出现在 tokens 文件）。
- [ ] 浅色 / 深色双主题全页面走查通过，切换无闪屏、无未覆盖区域。
- [ ] 正文与说明文字对比度 ≥ 4.5:1，非文本元素 ≥ 3:1（自动化扫描 + 抽样）。
- [ ] 四化同时具备颜色与文字标识，去色截图下仍可区分（色盲友好）。
- [ ] 移动端 320px 宽度下十二宫格不破版，最小触控区 ≥ 44×44dp。
- [ ] 系统字号放大至 120% 不破版；`prefers-reduced-motion` 下动效关闭。
- [ ] 命盘导出 PNG 在浅色背景下可读，深色主题导出时自动切换为浅色盘面。

### 15.4 增长与数据验收

- [ ] 词条页（≥ 30 个）可被搜索引擎正常抓取：首屏 HTML 含完整正文内容（用抓取工具验证）。
- [ ] 每页具备独立 `title` / `description` 与结构化数据；`sitemap.xml` 已提交。
- [ ] 核心漏斗埋点（12.2 全表）100% 上报，数据看板可查。
- [ ] 埋点审计通过：事件属性中不存在出生时间、经纬度、姓名、备注等敏感字段。
- [ ] 设置页「拒绝匿名统计」开关生效，关闭后不再上报行为事件。
- [ ] 分享图片不含姓名与具体出生日期；分享链接无法还原出姓名或备注。
- [ ] 分享页可直接还原命盘，无需注册/登录。

### 15.5 状态与边界验收

- [ ] 7.3 定义的六种状态在命盘页、盘库页、AI 解读页全部实现，无"只有理想态"的页面。
- [ ] 历法越界、闰月不存在、不知时辰三类边界均有明确提示文案与补救入口。
- [ ] 断网状态下仍可完成排盘与查看基础解读（AI 解读提示不可用并降级）。
- [ ] 本地存储被禁用时，页面可正常浏览，仅提示无法保存。

---

## 16. 待确认问题（Open Questions）

| # | 问题 | 建议 | 决策人 |
| --- | --- | --- | --- |
| Q1 | 采用哪个流派作为默认（三合派 vs 四化派） | 默认三合派（受众更广、星性解读更直观），提供四化派切换 | 产品 |
| Q2 | 晚子时换日默认策略 | 默认"晚子时归次日"（主流做法），在高级设置中可切 | 命理顾问 |
| Q3 | 「不知时辰」是否进入 P0 | 建议 P0 只做"提示 + 按午时排盘 + 强提示"，定盘流程放 P2 | 产品 |
| Q4 | AI 解读是自接 LLM 还是规则生成 + LLM 润色 | 建议规则生成事实层 + LLM 组织表达，降低幻觉 | 技术 |
| Q5 | 是否需要用户账号体系 | P0/P1 不需要（本地 + 链接分享）；P2 若做跨设备同步再引入 | 产品 |
| Q6 | 商业化节奏 | 建议 v1.0 后看数据再定，前期不做付费墙 | 产品 |
| Q7 | 头部印章印文用「易」还是「紫」 | 若品牌名含「易」则沿用「易」；否则建议「紫」，与紫微斗数品类一致 | 产品/设计 |
| Q8 | 六爻图、八卦符号在斗数产品中的定位 | 建议仅作装饰（空状态/分隔/方位标注），不作为功能控件，避免品类混淆 | 产品/设计 |
| Q9 | 斗数庙旺七级是否统一复用旺衰五态配色 | 建议按 6.2.4 映射表统一，避免两套标签体系并存 | 设计/命理顾问 |
| Q10 | AI 解读的免费额度与成本控制 | 建议每人每日 3 次基础解读，超出引导使用规则解读；深度报告作为后续付费点 | 产品 |
| **Q11** | **渲染方案：纯 SPA 还是 SSG + SPA 混合** | **建议 SSG 混合**（词条页/首页静态化 + 排盘页 SPA），纯 SPA 会导致 SEO 全废，直接影响 40–50% 的获客 | 技术（M0 必须决策） |
| Q12 | 是否接受"盘库仅存本地、无跨设备同步" | 建议 P0/P1 接受（换取零后端与隐私卖点）；P2 视留存数据再决定账号体系 | 产品 |

---

## 附录 A：术语表（节选）

| 术语 | 说明 |
| --- | --- |
| 命宫 | 命盘核心宫位，代表先天性格与格局 |
| 身宫 | 代表后天努力方向；与某宫重叠时强化该宫影响 |
| 三方四正 | 本宫 + 对宫 + 两个三合宫，判断宫位强弱的基本框架 |
| 十四主星 | 紫微、天机、太阳、武曲、天同、廉贞、天府、太阴、贪狼、巨门、天相、天梁、七杀、破军 |
| 四化 | 化禄（资源/人缘）、化权（掌控/竞争）、化科（名声/贵人）、化忌（执着/阻碍） |
| 五行局 | 水二/木三/金四/土五/火六局，决定起运年龄与紫微星排布 |
| 大限 | 十年一运，从命宫起顺/逆行 |
| 小限 | 一岁一运，以生年支定位 |
| 流年 | 当年的运势宫位与四化叠加 |
| 庙旺利陷 | 星曜在某宫位的亮度等级，影响力量强弱 |
| 格局 | 特定星曜组合形成的结构，如紫府朝垣、杀破狼、机月同梁 |

## 附录 B：参考资料

- 古籍：《紫微斗数全集》《紫微斗数全书》（十八飞星策天紫微斗数全集）
- 现代流派：中州派（王亭之）、钦天门四化派（蔡明宏）、三合派
- 开源参考：`iztro`（紫微斗数排盘 JS 库，用于交叉验证）、`lunar-typescript`（历法）
- 历法依据：紫金山天文台《中国天文年历》（用于二十四节气精度校验）

## 附录 C：设计令牌落地代码（`tokens.css`）

开发可直接引入，唯一允许出现 hex 色值的文件。

```css
:root {
  /* 字体 */
  --serif: "Songti SC", "STSong", "SimSun", "Noto Serif SC", serif;
  --kai: "Kaiti SC", "STKaiti", "KaiTi", "Noto Serif SC", serif;

  /* 形状与阴影 */
  --radius: 14px;
  --radius-sm: 6px;
  --shadow: 0 4px 20px rgba(60, 45, 25, 0.06);

  /* 浅色主题（宣纸，默认） */
  --bg: #f5f0e6;
  --bg-card: #fdfaf3;
  --ink: #2b2620;
  --ink-light: #6b6258;
  --cinnabar: #a03a2b;
  --cinnabar-dark: #842f22;
  --gold: #b78a3f;
  --border: #e2d8c4;

  /* 渐变 */
  --grad-btn: linear-gradient(135deg, #a03a2b, #842f22);
  --grad-line: linear-gradient(90deg, #a03a2b, #b78a3f, #a03a2b);

  /* 四化 */
  --mu-lu: #b78a3f;   /* 禄 金 */
  --mu-quan: #a03a2b; /* 权 朱砂 */
  --mu-ke: #3d6b4d;   /* 科 青 */
  --mu-ji: #2b2620;   /* 忌 墨 */

  /* 五行 */
  --wx-wood: #4a7c59;
  --wx-fire: #c0392b;
  --wx-earth: #b7791f;
  --wx-metal: #8d8d8d;
  --wx-water: #2c3e50;

  /* 旺衰五态（复用于斗数庙旺七级） */
  --st-wang-fg: #8a6522;  --st-wang-bg: rgba(183, 138, 63, 0.12);  --st-wang-bd: rgba(183, 138, 63, 0.4);
  --st-xiang-fg: #3d6b4d; --st-xiang-bg: rgba(74, 124, 89, 0.1);   --st-xiang-bd: rgba(74, 124, 89, 0.3);
  --st-xiu-fg: #3d5f7a;   --st-xiu-bg: rgba(74, 110, 140, 0.1);    --st-xiu-bd: rgba(74, 110, 140, 0.3);
  --st-qiu-fg: #8a5a22;   --st-qiu-bg: rgba(160, 102, 42, 0.1);    --st-qiu-bd: rgba(160, 102, 42, 0.3);
  --st-shuai-fg: #6b6258; --st-shuai-bg: rgba(107, 98, 88, 0.1);   --st-shuai-bd: rgba(107, 98, 88, 0.3);
}

[data-theme="dark"] {
  --bg: #1e1a16;
  --bg-card: #2a241d;
  --ink: #e8dfcf;
  --ink-light: #b8ad9a;
  --cinnabar: #d06a58;
  --cinnabar-dark: #b85344;
  --gold: #c9a35f;
  --border: #42392c;
  --shadow: 0 4px 20px rgba(0, 0, 0, 0.35);

  --mu-lu: #c9a35f;
  --mu-quan: #d06a58;
  --mu-ke: #5d9a72;
  --mu-ji: #b8ad9a;
}

/* 主题切换仅过渡颜色，禁止 transition: all */
.theme-transition,
.theme-transition * {
  transition: background-color 200ms ease, color 200ms ease, border-color 200ms ease;
}

@media (prefers-reduced-motion: reduce) {
  .theme-transition,
  .theme-transition * { transition: none; }
}
```

**防闪屏**：在 `<head>` 内联同步脚本读取 localStorage 并设置 `data-theme`，避免首屏白闪。

```html
<script>
  (function () {
    var t = localStorage.getItem('theme');
    if (!t) t = matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
    document.documentElement.setAttribute('data-theme', t);
  })();
</script>
```

## 附录 D：命名与 slug 规范

SEO 收录与代码一致性都依赖统一命名，本附录为**强制约定**。

### D.1 星曜 slug（小写拼音，连字符分隔）

| 星曜 | slug | 星曜 | slug |
| --- | --- | --- | --- |
| 紫微 | `zi-wei` | 天府 | `tian-fu` |
| 天机 | `tian-ji` | 太阴 | `tai-yin` |
| 太阳 | `tai-yang` | 贪狼 | `tan-lang` |
| 武曲 | `wu-qu` | 巨门 | `ju-men` |
| 天同 | `tian-tong` | 天相 | `tian-xiang` |
| 廉贞 | `lian-zhen` | 天梁 | `tian-liang` |
| | | 七杀 | `qi-sha` |
| | | 破军 | `po-jun` |

辅曜与杂曜沿用同一规则：`zuo-fu` 左辅、`you-bi` 右弼、`wen-chang` 文昌、`wen-qu` 文曲、`tian-kui` 天魁、`tian-yue` 天钺、`lu-cun` 禄存、`tian-ma` 天马、`qing-yang` 擎羊、`tuo-luo` 陀罗、`huo-xing` 火星、`ling-xing` 铃星、`di-kong` 地空、`di-jie` 地劫。

### D.2 宫位 slug

`ming-gong` 命宫 · `xiong-di` 兄弟 · `fu-qi` 夫妻 · `zi-nv` 子女 · `cai-bo` 财帛 · `ji-e` 疾厄 · `qian-yi` 迁移 · `jiao-you` 交友 · `guan-lu` 官禄 · `tian-zhai` 田宅 · `fu-de` 福德 · `fu-mu` 父母

### D.3 路由规范

```
/wiki/{category}/{slug}
category ∈ { star, palace, pattern, combo }
```

示例：`/wiki/star/tian-tong`、`/wiki/palace/cai-bo`、`/wiki/pattern/zi-fu-chao-yuan`、`/wiki/combo/tong-yin`

- 全小写、连字符分隔、不含中文与动态参数以外的特殊字符。
- **上线后禁止修改 slug**——修改等同于放弃已被收录的页面；确需变更时必须保留 301 跳转。

### D.4 代码命名约定

| 场景 | 约定 | 理由 |
| --- | --- | --- |
| 星曜内部标识 | **中文原名**（如 `'紫微'`） | 与规则表逐条对照，便于调试、日志与勘误定位 |
| URL / i18n key | slug（如 `zi-wei`） | URL 友好、便于国际化 |
| 宫位内部标识 | 中文原名（如 `'财帛'`） | 同上 |
| 四化 | 单字 `'禄' / '权' / '科' / '忌'` | 与古籍一致，避免翻译损耗 |

**术语一致性**：全产品统一使用「交友宫」（不使用「奴仆宫」）、「官禄宫」（不使用「事业宫」），杜绝对用户展示两种叫法。
